import {
    QueryResultRowType,
    TaggedTemplateLiteralInvocationType,
} from 'slonik';
import { Brand } from '../brand';

export type UtcDateString = Brand<string, 'utc_date_str'>;
export type Uuid = Brand<string, 'uuid'>;

